export enum GENDERS {
  MALE = 'MALE',
  FEMALE = 'FEMALE',
}

export enum STATUS {
  AVAILABLE = 1,
  RESERVED = 2,
  SOLD = 3,
}

export enum TRANSACTION_TYPES {
  SELL = 1,
  RESERVATION = 2,
}

export enum TRANSACTION_STATUS {
  PENDING = 1,
  ACCEPTED = 2,
  REJECTED = 3,
  CLOSED = 4,
}
